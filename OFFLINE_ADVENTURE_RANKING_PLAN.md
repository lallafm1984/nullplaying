# 오프라인 모험·랭킹 시스템 v2

## 목표

- 짧은 일일 접속 또는 자발적인 보상형 광고로 최대 24시간의 미접속 성장 시간을 충전한다.
- 누적 성장을 명예로 보여주면서도 후발 이용자가 주간 경쟁에 참여할 수 있게 한다.
- 전투력, 광고 시청 횟수, 과금이 다시 더 큰 성장 보상을 만드는 순환은 만들지 않는다.

## 오프라인 모험 게이지

### 앱 실행 중 충전

- 앱이 실제 foreground인 시간만 누적한다.
- 0%에서 100%까지 600,000ms(10분)이며, 100%는 24시간이다.
- 충전 비율은 곧 사용할 수 있는 오프라인 성장 시간이다. 25%면 6시간, 50%면 12시간이다.
- 100%에 도달해도 즉시 소비하지 않고 최대 24시간을 저장한다.
- 안내 문구는 `모험을 지켜보는 동안 오프라인 시간이 충전됩니다`로 표시한다.

### 앱 종료 중 소비

- foreground 전투는 게이지와 관계없이 항상 진행한다.
- background/offline 성장 시간만 저장 게이지에서 1:1로 차감한다.
- 가득 차지 않아도 충전된 시간만큼 즉시 사용할 수 있다.
- 저장 시간이 모두 소진되면 이후 미접속 구간은 성장하지 않는다.

### 미접속 정산

- 저장된 오프라인 시간만큼 기존 시간 기반 정산을 그대로 실행한다.
- 저장 시간 소진 이후 구간은 보상을 만들지 않고 전투 타임라인 전체를 정지 시간만큼 앞으로 이동한다.
- `lastSettledAt`만 바꾸지 않는다. 그렇게 하면 과거 공격 이벤트가 다음 실행에서 한꺼번에 처리된다.

## 보상형 광고

- CTA는 `광고로 충전`, 확인 버튼은 `광고 보고 24시간 충전`으로 보상을 명확하게 표시한다.
- 광고는 사용자의 직접 선택으로만 표시한다.
- `onUserEarnedReward`를 받은 경우에만 저장 시간을 24시간으로 채운다.
- 광고를 닫거나 로드·표시에 실패한 경우에는 충전하지 않는다.
- 광고 재생 중에는 10분 자연 충전을 멈춘다.
- 요청 ID를 저장해 같은 보상 콜백을 두 번 처리하지 않는다.
- 현재 개발 빌드는 Google 공식 rewarded 테스트 광고 단위를 사용한다.
- 서버 연결 단계에서는 AdMob SSV와 서버 시각으로 보상을 검증한다.

## 전투력 공식 v1

```text
능력 전투력 = roundHalfUp(주능력치 × 0.7 + 부능력치 × 0.3)
전체 전투력 = 능력 전투력 + 장착 장비력 평균
```

| 직업 | 주능력치 | 부능력치 |
|---|---|---|
| 전사 | STR | CON |
| 도적 | DEX | STR |
| 순찰자 | DEX | WIS |
| 마법사 | INT | WIS |
| 성직자 | WIS | INT |
| 성기사 | STR | CHA |

- 레벨, HP/MP, 희귀도, 스킬 수, 직업 보정 계수는 추가하지 않는다.
- 표시와 서버 계산에 `formulaVersion=1`을 사용한다.
- 서버는 클라이언트가 제출한 전체 전투력을 신뢰하지 않고 능력치와 여섯 장비 슬롯 값으로 재계산한다.

## 랭킹

### 전체 전투력

- 검증된 현재 전투력 내림차순이다.
- 누적 성장과 명예를 보여주는 기록이며 전투력 보상은 지급하지 않는다.

### 이번 주 성장

- `현재 검증 전투력 - 주간 시작 검증 전투력` 내림차순이다.
- 실제 반복 경쟁과 리텐션의 중심이다.
- 보상은 칭호, 프로필 테두리, 이름 색상처럼 전투력에 영향을 주지 않는 항목으로 제한한다.

### 동률

- 점수가 같으면 같은 순위를 표시한다.
- 페이지 순서만 `달성 서버 시각 ASC`, `characterId ASC`로 안정화한다.

## DB 연결 계약

전체 랭킹 행:

```text
rank, characterId, name, class, level, combatPower,
formulaVersion, powerAchievedAt, verifiedAt, isMe
```

주간 랭킹 추가 필드:

```text
seasonId, startPower, gainedPower
```

- 커서는 `(score, achievedAt, characterId)`를 사용한다.
- 서버 도입 전에는 가짜 사용자를 만들지 않고 `온라인 랭킹 준비 중`만 표시한다.
- 실제 경쟁 도입 전에는 서버 권위 성장 정산 또는 최소 Play Integrity와 성장 속도 이상 탐지가 필요하다.

## 저장 마이그레이션

- 저장 스키마 7 이하는 업데이트 전까지의 미접속 시간을 기존 방식으로 한 번 정산한다.
- 정산 직후 기존 캐릭터에 첫 24시간 무료 잔액을 제공한다.
- 스키마 8의 남은 활성 시간은 같은 길이의 오프라인 잔액으로, 10분 충전량은 0~24시간 비율로 변환한다.
- 신규 캐릭터도 첫 24시간 무료 잔액으로 시작한다.
- 레벨, 능력치, 장비, 스킬, 퀘스트 진행은 변경하지 않는다.

## 후속 검증 지표

- 일일 10분 자연 충전 완료율
- 광고 CTA 노출 대비 시작·완주·보상 지급률
- 24시간 활성 만료 후 24시간 이내 재방문율
- 전체 랭킹 조회율과 주간 랭킹 재방문율
- 광고 이용자와 비이용자의 주간 전투력 증가량 분포
- 신규 이용자의 1주·4주 랭킹 잔존율
