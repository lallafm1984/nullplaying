# NULL PLAYING 랭킹 UI MVP 기획

작성일: 2026-08-15

이 문서는 `OFFLINE_ADVENTURE_RANKING_PLAN.md`의 랭킹 부분을 현재 UI 작업 범위에 맞게 구체화한다. 오프라인 모험·광고 정책은 변경하지 않는다.

## 한 줄 정의

랭킹은 접속할 때 한 번 받은 전투력 순위를 읽기 전용으로 보여주는 명예 기록이다. 순위에 따라 호칭만 표시하며 능력치, 재화, 드롭률, 프로필 효과 같은 혜택은 주지 않는다.

## 이번 MVP에서 결정한 것

- 진입 위치: `캐릭터` 화면 최상단의 `모험가 랭킹` 전체 폭 메뉴
- 화면 형태: 뒤로가기가 있는 전용 랭킹 화면과 고정 하단 메뉴, 약 0.2초의 짧은 열기·닫기 전환
- 랭킹 기준: 현재 엔진의 `displayCombatPower()` 내림차순
- 갱신 방식: 오프라인 정산과 저장이 끝난 뒤 접속 세션당 한 번
- 미지원: 실시간 갱신, 탭 진입 시 재조회, 폴링, 웹소켓, 수동 새로고침
- 공개 정보: 순위, 닉네임, 직업, 레벨, 전투력, 호칭
- 효과: 호칭은 표시 전용이며 게임 상태에 저장하거나 전투 계산에 사용하지 않음

하단 5개 메뉴는 핵심 플레이 동선으로 유지한다. 랭킹을 여섯 번째 고정 메뉴로 넣지 않는 이유는 360dp 화면의 터치 폭을 줄이지 않고, 랭킹을 캐릭터 정체성의 확장으로 연결하기 위해서다.

## 호칭

| 순위 | 호칭 |
|---|---|
| 1위 | 왕좌의 모험가 |
| 2~3위 | 전설의 선봉 |
| 4~10위 | 황금 개척자 |
| 11~50위 | 은빛 추적자 |
| 51~100위 | 청동 길잡이 |
| 101위 이하 | 여정의 도전자 |
| 미집계 | 순위 집계 중 |

동점자는 같은 순위와 같은 호칭을 받는다. 순위 표시는 `1, 2, 2, 4` 경쟁 순위 방식을 사용하고, 같은 점수의 화면 순서만 `달성 시각 ASC`, `characterId ASC`로 안정화한다.

## 화면 위계

1. 뒤로가기, `모험가 랭킹`, 마지막 집계 시각
2. `내 순위` 고정 카드: 순위, 닉네임, 호칭, 전투력
3. 전체 랭킹 목록: 참가 인원, 정렬 기준, 순위 / 이름·직업·호칭 / 전투력

상위 3명은 행의 순위 색만 다르게 한다. 큰 시상대, 상승·하락 화살표, 라이브 점, 카운트다운, 다른 사용자 프로필 이동은 데이터 계약이 없는 동안 넣지 않는다.

## UI 상태

- `Loading`: 랭킹을 불러오는 중
- `Content`: 현재 접속에서 받은 스냅샷
- `Empty`: 집계 데이터가 없거나 서버 연결 전
- `Error`: 새 스냅샷을 받지 못함
- `Error + cache`: 이전 스냅샷과 마지막 갱신 시각을 함께 표시

현재 단계의 현실적인 참가자 목록은 디버그 빌드에서만 UI 검수용 샘플로 표시하고, 샘플 출처는 데이터 모델 내부에 유지한다. 릴리스 빌드는 가짜 목록 대신 `아직 집계된 순위가 없습니다` 빈 상태를 표시한다. 화면에는 개발·서버 연결 상태나 혜택 부재를 설명하는 문구를 노출하지 않는다.

## UI 데이터 계약

```text
RankingSnapshot
  snapshotId
  source                 // MOCK, CACHE, REMOTE
  fetchedAtEpochMillis
  formulaVersion
  totalParticipants
  entries
  myEntry                // 첫 페이지 밖이어도 별도 제공

RankingEntry
  rank                   // 목록 index로 다시 계산하지 않음
  characterId            // 외부에 노출 가능한 불투명 ID
  displayName
  heroClass
  level
  combatPower
  honorific
  achievedAtEpochMillis
  verifiedAtEpochMillis
  isMe
```

디버그 샘플은 아직 서버 공식 버전이 없으므로 `formulaVersion=0`을 사용한다. `1`을 임의로 재사용하지 않는다.

서버가 연결되면 클라이언트가 제출한 전투력을 그대로 신뢰하지 않는다. 현재 엔진의 정규화가 포함된 `displayCombatPower()`와 동일한 공식 버전으로 서버가 검증해야 한다.

## 이번에 제외한 것

- `이번 주 성장` 보드: 접속 시간과 광고 충전량의 영향을 받아 동일 조건 경쟁이라고 부르기 어려움
- 프로필 테두리, 이름 색상, 배지 장착: 이번 요구의 `호칭만` 범위를 넘음
- 보상 수령, 시즌 보상, 능력치 보정
- DB, 인증, 페이지네이션, 신고·차단

주간 보드는 서버 권위 정산, 시즌 경계 분할, 일일 인정 성장시간 같은 공정성 정책을 먼저 정한 뒤 별도 단계에서 추가한다.

## 완료 조건

- 캐릭터 화면의 48dp 이상 진입 메뉴로 랭킹 화면을 열 수 있다.
- 화면 내 뒤로가기, 시스템 뒤로가기, 하단의 다른 메뉴 이동이 모두 동작한다.
- 랭킹 본문은 열기·닫기 때 짧게 이동하며, 광고와 하단 메뉴는 고정된다.
- 한 접속 동안 게임 전투력이 변해도 랭킹 점수와 기준 시각은 바뀌지 않는다.
- 내 순위 카드가 목록 스크롤과 무관하게 보인다.
- 동률 순위, 호칭 경계, 안정 정렬, 샘플/릴리스 상태가 단위 테스트로 검증된다.
- 긴 한글 이름, 1,000위 이상, 큰 전투력에서도 순위와 점수가 잘리지 않는다.
- 샘플 데이터는 디버그 빌드에만 존재하고, 랭킹 UI가 저장·전투·오프라인 정산을 변경하지 않는다.
