# AlarmQuest 랭킹 시스템 상세 분석 및 개선 기획

작성일: 2026-08-23
상태: 기획 초안 — 구현 및 원격 DB 변경 없음

## 1. 결론

현재 랭킹은 화면 데모 단계를 넘어 익명 로그인, 캐릭터별 전투력 제출, 전체 Top 100 조회, 내 순위 표시까지 연결된 MVP다. 그러나 지금 상태를 실제 경쟁 기능으로 공개하면 클라이언트가 임의의 전투력을 제출할 수 있고, 한 사용자의 세 슬롯이 동시에 전체 순위에 들어가며, 문서와 서버의 동점 순위 방식도 서로 다르다.

따라서 다음 구현의 우선순위는 화면 장식이나 보상 추가가 아니라 아래 세 가지다.

1. 클라이언트 `공식 전투력`을 신뢰하기 위한 다층 검증
2. 저장 슬롯의 `모든 캐릭터`가 각각 공정하게 참가하는 캐릭터 단위 랭킹
3. 클라이언트 전투력을 신뢰할 수 있도록 변조 비용과 이상 탐지를 겹겹이 적용

권장 제품 구조는 직업별 탭 없이 `전체 전투력 Top 1,000` 단일 목록으로 제공하는 것이다. 모든 캐릭터가 동일한 전투력 규칙으로 참가하며, `주간 성장`은 이번 범위에서 제외하고 추후 별도로 판단한다.

## 2. 현재 구현 확인 결과

### 클라이언트

- 진입점: 캐릭터 화면의 `모험가 랭킹` 메뉴와 메인 헤더의 전체 순위
- 제출 시점: 사용자가 랭킹 화면을 열 때 `submitAndFetchRanking(activeSlotId)` 실행
- 제출 값: 닉네임, 직업, 레벨, `displayCombatPower()` 결과
- 조회: RPC `get_leaderboard(p_slot_id, p_limit=100)`
- 표시: 내 순위 고정 카드 + 전체 Top 100
- 캐시: 동일 Compose 생명주기 안에서 조회 실패 시 직전 스냅샷 표시. 앱 재시작 후 영속 캐시는 없음
- 인증: 기기 로컬에 저장된 Supabase 익명 세션
- 공개 정보: 순위, 닉네임, 직업, 레벨, 전투력, 순위 호칭

### 서버

- 테이블 키: `(user_id, slot_id)` — 사용자당 최대 세 행
- 정렬 인덱스: `combat_power DESC, achieved_at ASC, user_id, slot_id`
- RLS: 인증 사용자는 전체 조회 가능, 자기 `user_id` 행만 삽입·수정 가능
- 현재 RPC: 모든 슬롯 행을 하나의 전체 보드에 넣고 `dense_rank()`로 순위 계산
- 내 행: Top 100 밖이어도 요청한 슬롯 행을 함께 반환
- 시각: 전투력이 증가하면 `achieved_at` 갱신, 그 외 수정은 기존 시각 유지
- 원격 상태: 로컬 마이그레이션 `202608230001~003`과 원격 적용 목록 일치 확인

### 전투력 공식

현재 앱의 표시 전투력은 `characterStatPower(state) + averageEquipmentPower(state)`다. 서버에는 결과 숫자만 전송되며 이를 재계산할 능력치·장비 원본이나 공식 버전은 전송되지 않는다.

## 3. 핵심 문제와 위험도

| 우선도 | 문제 | 현재 영향 | 개선 원칙 |
|---|---|---|---|
| P0 | 클라이언트 제출 전투력을 아무 검증 없이 저장 | 변조 앱·로컬 저장 조작으로 임의 1위 가능 | Integrity·산술 일관성·성장 개연성을 통과한 제출만 공개 순위에 반영 |
| P0 | 서버에 `formula_version`, 검증 상태가 없음 | 공식 변경 시 구점수와 신점수 혼합 | 점수마다 공식 버전과 검증 결과 기록 |
| P0 | 캐릭터별 참가 원칙과 식별 계약이 명시되지 않음 | 슬롯 삭제·재생성 시 과거 캐릭터와 새 캐릭터의 정체성이 섞일 수 있음 | 슬롯 번호가 아닌 영구 `character_id`로 모든 캐릭터를 개별 참가시킴 |
| P1 | 기획은 경쟁 순위 `1,2,2,4`, 서버는 밀집 순위 `1,2,2,3` | 호칭 경계와 실제 표시 불일치 | `rank()`로 통일 권장 |
| P1 | 점수가 내려가도 서버 행을 덮어씀 | 장비 교체 시 최고 기록이 사라지고 달성 시각 의미가 흐려짐 | 현재 전투력과 최고 전투력을 분리하거나 정책을 명시 |
| P1 | 익명 계정이 기기 세션에 묶임 | 재설치·기기 변경 시 랭킹 정체성 상실 | 계정 연결 또는 복구 수단 마련 |
| P1 | 닉네임 중복·금칙어·신고 정책 없음 | 사칭, 욕설, 운영 분쟁 | 정규화, 금칙어, 신고/숨김, 운영 제재 계약 |
| P1 | Top 100만 표시 | 중상위권 사용자의 비교·성장 체감 부족 | 전체 Top 1,000까지 제공하고 내 순위 바로 이동 지원 |
| P2 | 갱신이 화면 진입에 의존 | 랭킹을 열지 않은 강한 캐릭터는 미집계 | 저장/정산 완료 후 제한적 자동 제출 + 화면 수동 재조회 |
| P2 | 영속 캐시와 스냅샷 만료 정책 없음 | 네트워크 장애 때 헤더가 재시작마다 미집계 | 마지막 정상 스냅샷과 만료 시각 로컬 저장 |
| P2 | `formulaVersion=1`이 UI에서 하드코딩 | 서버 데이터와 실질적으로 연결되지 않음 | RPC가 버전을 반환하고 클라이언트가 그대로 표시/판단 |
| P2 | 참가자 정의가 불명확 | 캐릭터 수와 사용자 수가 혼동될 수 있음 | 화면의 `참가 캐릭터`는 캐릭터 행 수, 운영 지표의 `참가 사용자`는 고유 사용자 수로 분리 |

## 4. 제품 규칙 제안

### 4.1 랭킹 보드

#### 전체 탭 — 전체 전투력

- 목적: 장기 성장과 수집 성과를 보여주는 영구 명예 보드
- 단위: 저장된 모든 캐릭터. 한 사용자의 1~3번 슬롯 캐릭터가 각각 참가 가능
- 식별자: `slot_id`와 별도로 생성 시 고정되는 불투명 `character_id` 사용
- 동률: 경쟁 순위 `rank()` 사용 (`1, 2, 2, 4`)
- 동점 화면 순서: `score_achieved_at ASC`, `character_id ASC`
- 보상: 전투력·재화 보상 없음. 순위 구간에 따른 명예 호칭 부여
- 표시: Top 1,000, 내 순위, 총 참가 캐릭터 수
- 참가 조건: 레벨 20 이상

한 사용자가 여러 캐릭터를 육성한 노력도 각각 인정한다. 보상이 없는 명예 경쟁이므로 계정별 출전 수를 제한하지 않는다. 다만 같은 슬롯을 삭제하고 다시 만든 경우는 새 `character_id`를 부여하며, 삭제된 캐릭터의 공개 기록은 삭제 또는 비공개 처리해 유령 캐릭터가 계속 순위를 점유하지 않게 한다.

레벨 20 미만 캐릭터의 제출 데이터는 공개 순위 계산에서 제외한다. 이 조건은 클라이언트 표시뿐 아니라 서버 RPC와 공개 뷰에서 강제한다. 레벨 20에 도달하면 다음 정상 제출부터 전체 탭과 해당 직업 탭에 함께 참가한다. 미달 캐릭터에게는 순위 대신 `랭킹 참가까지 Lv.20`과 현재 레벨을 표시한다.

#### 추후 검토 — 이번 주 성장

이번 기획과 구현 범위에서는 제외한다. 전체 Top 1,000 랭킹이 안정화된 뒤 필요성과 공정성 정책을 새로 검토한다.

### 4.2 현재 점수와 최고 기록

권장은 두 값을 분리하는 것이다.

- `current_power`: 현재 장착 상태의 검증 전투력. 전체 랭킹 기준
- `best_power`: 과거 최고 검증 전투력. 개인 프로필 기록과 시즌 회고용

전체 랭킹은 `current_power`를 쓰는 편이 장비를 해제한 캐릭터가 과거 장비 구성으로 계속 상위에 남는 문제를 피한다. 단, 갱신 주기는 악용과 서버 비용을 고려해 최소 간격을 둔다. 개인 카드에는 `최고 전투력`을 별도 표시할 수 있다.

### 4.3 갱신 정책

#### 조회 — 앱 시작 시 한 번

- 앱 프로세스의 첫 정상 시작에서 랭킹 번들 RPC를 1회 호출한다.
- 응답에는 `전체 Top 1,000`과 내 모든 레벨 20 이상 캐릭터의 공식 순위를 한 스냅샷으로 묶는다.
- 메인 헤더와 상세 랭킹도 같은 `snapshot_id`를 사용해 서로 다른 순위를 표시하지 않는다.
- 조회 성공 스냅샷은 로컬에 저장한다. 시작 조회 실패 시 캐시를 `이전 집계`로 표시하고 게임 진행은 막지 않는다.
- 같은 프로세스에서 화면을 다시 열거나 탭을 전환해도 재조회하지 않는다.
- 수동 새로고침과 실시간 폴링은 초기 버전에서 제공하지 않는다.

#### 화면 내 즉시 갱신 — 서버 통신 없음

시작 스냅샷은 서버 기준 원본으로 보존하고, 화면에는 별도의 `displaySnapshot`을 사용한다. 플레이 중 활성 캐릭터의 전투력이 변하면 내려받은 목록 안에서 그 캐릭터의 점수만 현재 로컬 전투력으로 바꾼 뒤 전체 경쟁 순위를 즉시 다시 계산한다.

- 전투력 변화마다 네트워크를 호출하지 않는다.
- 메인 헤더, 내 순위 카드, 전체 목록과 호칭을 같은 로컬 계산 결과로 즉시 갱신한다.
- 다른 캐릭터의 점수는 시작 스냅샷 값으로 고정한다.
- 공식 서버 스냅샷은 덮어쓰지 않아 오류 복구와 표시 근거를 유지한다.
- 로컬 재계산 상태에는 `현재 전투력 기준` 표시를 붙이고, 서버에서 받은 값에는 `앱 시작 시 집계`를 표시한다.
- 로컬 순위와 호칭은 사용자 화면에서만 유효하다. 서버 제출과 검증이 끝나기 전 다른 사용자에게는 보이지 않는다.

정확한 로컬 순위를 계산할 수 있는 범위도 구분한다.

- 내려받은 전체 참가자가 1,000명 이하이면 모든 순위를 정확히 재계산할 수 있다.
- 참가자가 1,000명을 넘더라도 새 점수가 시작 Top 1,000 경계 안에 들어오면 `현재 목록 기준` 순위를 정확히 계산할 수 있다.
- Top 1,000 경계 밖에서는 서버의 모든 하위 점수를 받지 않았으므로 정확한 새 숫자를 만들지 않는다. 이때는 `1,000위 밖 · 순위 상승 중` 또는 마지막 공식 순위와 전투력 변화만 표시한다.
- 레벨 20을 플레이 중 처음 달성한 캐릭터도 같은 규칙으로 로컬 예상 순위를 보여주되, 다음 batch 제출 전까지는 `첫 집계 대기` 상태로 구분한다.

#### 제출 — 최종 변경분을 한 번에

앱을 열자마자 조회와 제출을 동시에 실행하지 않는다. 먼저 시작 스냅샷을 받아 화면 기준을 고정한다. 플레이 중에는 로컬 표시만 갱신하고 서버 제출용 변경분은 계속 로컬에 저장한다.

- 각 캐릭터에 마지막 정상 제출의 `ranking_fingerprint`를 로컬 저장한다.
- fingerprint는 `character_id, level, class, displayName, formulaVersion, combatPower, 검증용 능력치·장비 요약`으로 만든다.
- 레벨 20 이상이며 fingerprint가 달라진 캐릭터만 dirty로 표시한다.
- 앱이 background로 이동할 때 최신 dirty 캐릭터들을 pending batch로 먼저 영속 저장하고 단일 batch RPC를 1회 시도한다.
- OS 종료 등으로 전송하지 못한 pending batch는 다음 앱 시작에서 새 조회를 마친 뒤 1회 전송한다.
- 장시간 foreground 플레이 중에는 제출하지 않는다. 최종 전투력이 아닌 중간값을 올리고 이후 변경을 놓치는 문제를 피한다.
- 한 앱 프로세스에서는 정상 batch 제출을 최대 1회만 수행한다. 이미 제출한 뒤 추가 변화가 생기면 이를 새 pending batch로 저장해 다음 시작에 전송한다.
- 제출 응답은 성공/보류/거부 영수증만 받고 랭킹 목록을 다시 조회하지 않는다. 새 점수의 공식 순위는 다음 앱 시작 스냅샷에 반영한다.
- 변경이 없거나 레벨 20 미만뿐이면 제출하지 않는다.
- 서버는 사용자별 빈도 제한, nonce와 멱등 키를 적용한다.

#### 통신 예산

정상적인 한 앱 프로세스 기준 최대 통신은 `시작 조회 1회 + background 또는 이월된 변경 batch 제출 1회`다. 랭킹 화면을 여러 번 열거나 전투력이 여러 번 바뀌거나 목록을 오래 스크롤해도 추가 호출은 없다. 사용자 화면은 즉시 로컬 재계산하고, 다른 사용자에게 보이는 공식 순위는 다음 시작 스냅샷에서 확인한다.

## 5. 서버 권위와 부정행위 방어

### 목표: 클라이언트 전투력을 최대한 신뢰할 수 있는 구조

현재 게임은 성장 상태가 로컬에 있으므로 모든 플레이를 서버에서 재현하는 완전 서버 권위 구조는 변경 범위가 크다. 클라이언트가 계산한 전투력을 공식 점수로 제출하되, 단일 방어책에 의존하지 않고 아래 방어 계층을 겹쳐 신뢰도를 높인다. 클라이언트 소유 데이터의 변조를 100% 막을 수는 없으므로 목표는 `변조 비용 상승 + 위조 탐지 + 공개 제외 + 운영 복구`다.

1. 배포 무결성: Play App Signing, 난독화·최적화, 릴리스에서 디버그 기능 제거, 패키지·서명·버전 확인
2. 실행 무결성: Play Integrity 토큰을 서버에서 검증하고 앱·기기·계정 판정을 기록
3. 전송 무결성: 짧은 인증 세션, TLS, 서버 발급 nonce, 1회용 `submission_id`, 재전송 방지
4. 데이터 일관성: 총 전투력과 공식 버전, 레벨, 핵심 능력치, 장비 슬롯별 전투력을 함께 보내 산술 관계 교차 검사
5. 성장 개연성: 직전 정상 기록과 비교해 경과 시간 대비 레벨·능력치·장비·전투력 증가 상한 검사
6. 위험 기반 공개: 정상은 `verified`, 의심은 `provisional`, 명백한 위조는 `rejected`; 공개 보드에는 `verified`만 포함
7. 운영 방어: 급상승·상위권·반복 실패를 감사하고 점수 숨김·복구 이력을 보존

APK 안의 고정 비밀키로 payload에 서명하는 방식은 키 추출이 가능하므로 핵심 방어책으로 쓰지 않는다. 로컬 저장 파일의 단순 해시도 공격자가 값과 해시를 함께 바꿀 수 있어 단독 검증 수단이 될 수 없다.

### 제출 스냅샷

클라이언트는 전투력 총합과 검증용 최소 스냅샷을 함께 제출한다. 서버는 클라이언트 계산 결과를 기본 점수로 받아들이되 공식 산술 관계, 값 범위와 성장 속도를 교차 검사한다.

필수 필드 예시:

```text
submission_id, user_id, character_id, slot_id
formula_version, save_schema_version, app_version_code
hero_class, level
rankable_stats
equipped_item_ids_and_power
client_observed_at
integrity_token
```

서버 검증 항목:

- 공식 버전과 앱 최소 지원 버전
- Integrity 토큰의 nonce·패키지·서명·버전·요청 시각 일치
- 능력치·장비 슬롯 수·값 범위·카탈로그 ID
- 레벨 대비 비현실적 성장량과 직전 검증 기록 대비 증가 속도
- 중복 `submission_id`, 과도한 요청 빈도
- 음수, overflow, 알 수 없는 직업·아이템·스키마
- 검증 실패 기록은 공개 보드에서 제외하고 운영 감사 로그에 사유 코드만 보관

Play Integrity 판정 하나만으로 차단하지 않고 데이터 일관성과 성장 개연성을 함께 본다. 기기 환경 문제로 정상 사용자가 영구 배제되지 않도록 재시도와 운영 복구 경로를 둔다.

### 현실적인 단계적 전환

1. 즉시: 기존 보드를 `베타/집계 중` 성격으로 유지하고 경쟁 보상은 연결하지 않음
2. 서버 계약 v2: 공식 버전, 캐릭터 ID, 제출 ID, Integrity 판정, 검증 상태 도입
3. 범위 검증: 레벨/스탯/장비의 구조·상한·증가 속도 검증
4. 상위권·급상승 제출에 강화 검증과 운영 검토 적용
5. 필요성이 확인되면 장기적으로 서버 재계산 또는 서버 성장 원장으로 확장

## 6. 제안 데이터 모델

```text
ranking_submissions
  submission_id PK
  user_id
  character_id
  slot_id
  formula_version
  payload
  submitted_at
  verification_status
  rejection_reason_code
  verified_power
  verified_at

ranking_characters
  user_id + character_id PK
  slot_id
  display_name
  normalized_name
  hero_class
  level
  current_power
  best_power
  score_achieved_at
  verified_at
  formula_version
  visibility_status

ranking_profiles
  user_id PK
  name_moderation_status
  ranking_ban_until

ranking_seasons (주간 보드 도입 시)
  season_id PK
  starts_at
  ends_at
  formula_version
  status
```

조회 RPC는 원시 `user_id`를 앱에 반환하지 않고 외부용 불투명 `public_character_id`만 반환하는 편이 좋다.

권장 RPC:

```text
submit_ranking_snapshot(payload) -> verification receipt
get_power_leaderboard(scope, hero_class?, cursor?, limit?)
get_my_ranking_context(scope, hero_class?, neighbor_count)
report_ranking_name(public_character_id, reason)
```

## 7. UX 개선안

### 정보 구조

- 상단: `전체 랭킹` 제목, 마지막 집계 시각, 참가 캐릭터 수
- 내 카드: 현재 순위, 전투력, 직업, 마지막 집계 시각, 최고 기록
- 목록: 서버에서 받은 Top 1,000 단일 목록
- 화면 하단 또는 목록 위에 `최상단` / `내 순위` 빠른 이동 버튼 제공
- 갱신 실패: 기존 목록 유지 + 짧은 비차단 경고 + 다시 시도
- 미집계: 이유를 구분해 `첫 기록 등록`, `네트워크 연결`, `검증 중` 상태 제공

### 목록 렌더링과 스크롤

Top 1,000 데이터는 앱 시작 시 한 번 내려받지만 화면 행 1,000개를 미리 Composable이나 완전한 표시 모델로 만들지 않는다.

- 네트워크 DTO는 필수 필드만 가진 compact immutable list로 보관한다.
- Compose `LazyColumn`이 현재 보이는 행과 가까운 행만 구성한다.
- 호칭, 숫자 포맷, 색상, 접근성 문구는 행이 실제로 구성될 때 계산한다.
- 사용자가 아래로 스크롤하면 로컬 목록에서 다음 행이 계속 나타난다. 추가 서버 호출이 없는 `로컬 무한 스크롤`이다.
- 필요하면 UI 노출 구간을 100개씩 늘리되 데이터 원본은 이미 받은 Top 1,000을 사용한다.
- 각 행 key는 `public_character_id`, 이동 대상은 서버가 함께 준 `list_index`를 사용한다. 동점 순위 때문에 `rank - 1`을 인덱스로 사용하면 안 된다.

### 빠른 이동 버튼

- 두 버튼은 같은 자리에 계속 표시하되 현재 화면 상태에 따라 각각 독립적으로 활성화·비활성화한다. 버튼이 사라져 레이아웃이 흔들리게 하지 않는다.
- `최상단` 활성 조건: `firstVisibleItemIndex != 0`이거나 첫 항목의 scroll offset이 0이 아닐 때. 화면이 정확한 최상단이면 비활성화한다.
- `최상단` 동작: `animateScrollToItem(0)`으로 화면의 첫 항목과 시작 여백까지 정확히 복귀한다.
- `내 순위` 활성 조건: 현재 활성 캐릭터의 랭킹 행이 `visibleItemsInfo`에 없을 때. 행의 일부라도 화면에 보이면 비활성화한다.
- `내 순위` 동작: 활성 캐릭터의 `public_character_id`로 로컬 목록 인덱스를 찾고 해당 행이 화면 세로 중앙에 오도록 이동한다.
- 이동 애니메이션이 너무 길어지지 않도록 먼 거리에서는 먼저 근처로 즉시 점프한 뒤 짧게 중앙 정렬한다.
- 활성 캐릭터가 Top 1,000 밖이면 목록 안에 내 행이 없으므로 `내 순위 · 1,000위 밖` 버튼은 활성화한다. 누르면 LazyColumn 상단 항목으로 포함된 내 순위 카드가 화면 중앙에 오도록 이동한다. 내 순위 카드가 이미 보이면 비활성화한다.
- 레벨 20 미만이면 `내 순위` 버튼을 비활성화하고 `Lv.20부터 참가` 안내를 제공한다.
- 플레이 중 로컬 재계산으로 내 인덱스가 바뀌면 버튼도 새 위치를 사용한다.
- 활성화 판정은 `LazyListState.layoutInfo.visibleItemsInfo`를 `derivedStateOf`로 관찰해 불필요한 전체 목록 재구성을 막는다.
- 이동 직후 대상이 화면에 들어오면 해당 버튼은 즉시 비활성화되고, 다시 화면 밖으로 스크롤하면 활성화된다.
- TalkBack에는 각각 `랭킹 최상단으로 이동`, `내 순위로 이동` 동작을 제공하고 이동 후 대상 행에 초점을 전달한다.

### 호칭

호칭은 순위 보상의 역할을 대신하는 명예 표시다. 현재 순위가 속한 구간의 호칭을 자동 부여하고, 내 카드·랭킹 행·메인 헤더의 상세 접근성 문구에 일관되게 표시한다.

#### 권장 호칭 목록 — 여정과 명예

| 전체 순위 | 호칭 | 의도 |
|---|---|---|
| 1위 | 유일한 왕좌 | 한 명뿐인 최정상 |
| 2위 | 왕좌에 닿은 자 | 정상 바로 아래의 도전자 |
| 3위 | 천상의 수호자 | 최상위 세 명의 위엄 |
| 4위 | 전설의 선봉 | 전설 구간의 첫 자리 |
| 5위 | 별을 베는 자 | 압도적인 전투력의 상징 |
| 6위 | 불굴의 정복자 | 꺾이지 않는 상위 모험가 |
| 7위 | 황금의 개척자 | 새로운 경지를 연 모험가 |
| 8위 | 새벽의 추적자 | 정상을 끝까지 추격하는 자 |
| 9위 | 은빛의 영웅 | Top 10을 지킨 영웅 |
| 10위 | 별빛의 계승자 | Top 10의 명예를 이어받은 자 |
| 11~25위 | 황금의 선구자 | 최상위권 진입 단계 |
| 26~50위 | 은빛의 추적자 | 상위 50위의 성취 |
| 51~100위 | 청동의 길잡이 | Top 100 진입 성취 |
| 101~300위 | 별빛의 도전자 | 상위권을 추격하는 단계 |
| 301~1,000위 | 새벽의 모험가 | 성장 목표가 남은 중상위권 |
| 1,001위 이하 | 여정을 걷는 자 | 순위에 참가한 모든 캐릭터 인정 |
| 미집계 | 이름 없는 도전자 | 아직 공식 점수가 없는 상태 |

1~10위는 모두 고유 호칭을 받고, 11위부터는 범위 호칭을 받는다. 기존 AlarmQuest 호칭의 분위기를 유지하면서 100위 밖 사용자도 호칭을 받아 참가 자체의 만족감을 남긴다.

현재 단계에서는 호칭을 자동 표시하는 단순형으로 운영한다. 영구 수집·장착형 호칭은 시즌 시스템을 검토할 때 함께 판단한다.

### 개인정보·안전

- 닉네임 중복 허용 여부를 명시하고, 중복 시 직업/짧은 공개 ID로 구분
- 금칙어 정규화는 공백·기호·대소문자·유사문자를 고려
- 신고 결과를 기다리는 동안 개인 기기에서 즉시 숨김 가능
- 실제 사용자 ID, 기기 정보, 국가 정보는 랭킹 응답에 포함하지 않음
- 계정 삭제 시 랭킹·제출·신고 연결 데이터의 삭제 또는 익명화 규칙 마련

## 8. 운영 및 지표

### 운영 화면에 필요한 항목

- 상위 점수와 최근 급상승 기록
- 공식 버전별 참가자 수
- 검증 실패율과 사유 분포
- 사용자별 제출 빈도와 반복 실패
- 신고된 닉네임, 숨김/복구, 랭킹 제한 이력
- 공식 변경 전후 점수 분포와 순위 변동

### 성공 지표

- 랭킹 진입률: 캐릭터 화면 노출 사용자 중 진입 비율
- 정상 집계율: 제출 시도 중 검증 완료 비율
- 내 순위 바로 이동 사용 후 1일/7일 재방문
- 신규 사용자의 첫 집계까지 걸린 시간
- 전체 참가 캐릭터 수와 전투력 분위수
- 검증 실패율, 신고율, 운영 처리시간

순위 상승 자체를 리텐션의 원인으로 단정하지 않고, 기능 도입 전후 코호트와 게임 진행 단계별로 비교한다.

## 9. 구현 로드맵

### Phase 0 — 정책 고정

- 모든 캐릭터의 개별 참가와 영구 `character_id` 확정
- 레벨 20 이상 참가 조건 확정
- `rank()` 경쟁 순위 확정
- 현재/최고 전투력 분리 확정
- 순위 구간별 자동 호칭, 전투력·재화 보상 없음 확정
- 공식 버전 변경과 구버전 처리 정책 확정

완료 기준: 제품 규칙과 서버 계약에서 모순이 없음.

### Phase 1 — 서버 계약 v2와 안전한 마이그레이션

- 기존 테이블을 바로 파괴하지 않고 v2 테이블/RPC 추가
- 공식 버전, 검증 상태, 외부 공개 캐릭터 ID, Integrity 결과 추가
- 개발 데이터는 출시 전 삭제하고 깨끗한 운영 데이터베이스에서 시작
- `rank()`와 전체 Top 1,000 조회 구현
- 전체 Top 1,000과 내 캐릭터 공식 순위·목록 인덱스를 반환하는 시작 스냅샷 RPC 구현
- 레벨 20 미만이 모든 공개 랭킹 쿼리에서 제외되는지 검증
- RLS, RPC 권한, 입력 범위, rate limit 테스트

완료 기준: 임의 전투력 직접 upsert가 공개 보드에 반영되지 않음.

### Phase 2 — 앱 연동과 복원력

- v2 제출 영수증과 조회 모델 적용
- 영속 캐시, 만료 시각, 검증 중/실패 상태 추가
- 전체 Top 1,000 로컬 무한 스크롤과 `최상단` / `내 순위` 이동 UI
- 서버 원본 스냅샷과 로컬 `displaySnapshot`을 분리하고 전투력 변화 시 즉시 재계산
- 앱 시작 조회 1회와 dirty 캐릭터 batch 제출 1회의 통신 예산 적용
- fingerprint, pending batch, 제출 영수증과 재시도 정책 구현
- 긴 이름, 큰 순위, 오프라인, 세션 만료, 슬롯 변경 테스트

완료 기준: 재시작·네트워크 실패·Top 1,000 밖에서도 사용자 상태가 명확함.

### Phase 3 — 계정·운영

- 익명 계정의 Google 계정 연결 또는 복구 경로
- 닉네임 정규화·금칙어·신고·숨김
- 운영 감사 조회와 랭킹 제한
- 삭제 요청의 연쇄 삭제/익명화 검증

완료 기준: 재설치 복구와 악성 닉네임/점수 대응 절차가 실제로 작동함.

### 별도 후속 — 주간 성장

현재 로드맵에서는 제외한다. 전체 Top 1,000 랭킹 출시 후 실제 이용 지표를 보고 새 기획으로 판단한다.

## 10. 테스트 및 출시 게이트

### 서버

- 다른 사용자의 행 삽입·수정·삭제 거부
- 원시 테이블 직접 upsert로 공개 점수 생성 불가
- 동일 점수 `1,2,2,4`, 달성 시각 안정 정렬
- 1~10위 개별 호칭과 11위 이하 범위 경계 정확성
- 레벨 19는 미집계, 레벨 20은 다음 정상 제출부터 집계
- 같은 사용자의 서로 다른 캐릭터가 각각 정확히 참가자 수에 포함
- 슬롯 삭제·재생성 시 새 캐릭터가 이전 공개 기록을 이어받지 않음
- Top 1,000 경계와 Top 1,000 밖 내 공식 순위 정확성
- 공식 버전 혼합 방지, overflow, 음수, 잘못된 슬롯/직업 거부
- 동시 제출, 중복 제출, rate limit, 계정 삭제 검증

### 앱

- 정산 완료 전 제출하지 않음
- 앱 시작 조회 후 화면 재진입·전체 목록 스크롤에서 추가 호출이 발생하지 않음
- 변경 없음·레벨 20 미만에서는 batch 제출이 발생하지 않음
- 여러 dirty 캐릭터가 한 batch에 담기고 프로세스당 1회를 넘지 않음
- Top 1,000 안에서는 전투력 상승·하락에 따라 로컬 순위와 호칭이 즉시 일관되게 변경됨
- Top 1,000 밖에서는 근거 없는 정확한 새 순위 숫자를 만들지 않음
- 1,000행을 선구성하지 않고 현재 보이는 LazyColumn 행만 구성함
- 동점 순위에서 `내 순위`가 rank가 아닌 캐릭터 목록 인덱스로 정확히 이동함
- `최상단`과 `내 순위` 이동 후 대상 행이 보이고 접근성 초점이 전달됨
- 정확한 최상단에서는 `최상단`이 비활성화되고 1px 이상 스크롤하면 활성화됨
- 내 랭킹 행 또는 Top 1,000 밖 내 카드가 보이면 `내 순위`가 비활성화되고 화면 밖이면 활성화됨
- 두 버튼 중 하나만 조건을 만족할 때 다른 버튼 상태에 영향을 주지 않음
- 공식 스냅샷과 로컬 현재 전투력 기준 상태가 시각·접근성 문구에서 구분됨
- 제출 후 즉시 재조회하지 않고 다음 시작 스냅샷에서 새 순위가 반영됨
- 슬롯 전환 중 잘못된 캐릭터를 `isMe`로 표시하지 않음
- 캐시/새 데이터/검증 중/오류 상태 접근성 문구 일치
- 360dp, 큰 글꼴, 긴 한글 이름, 4자리 이상 순위에서 잘림 없음
- 헤더 순위와 상세 화면 순위가 같은 스냅샷을 사용
- 랭킹 실패가 저장·전투·오프라인 정산을 막지 않음

### 출시

- 스테이징에서 조작 제출과 권한 우회 테스트
- 기존 v1 앱과 v2 서버의 호환/차단 정책 확인
- 순위·참가자·공식 버전 분포 모니터링
- 롤백 시 v1 직접 쓰기 권한이 다시 열리지 않도록 확인

## 11. 이번 기획에서 확정 권장하는 결정

다음 구현은 아래 기본값으로 진행하는 것이 가장 안전하다.

1. 전체 랭킹은 `모든 캐릭터가 각각 참가`
2. 점수는 다층 무결성·일관성·성장 검증을 통과한 `클라이언트 현재 전투력`
3. 동점은 `1, 2, 2, 4` 경쟁 순위
4. 직업별 탭 없이 `전체 Top 1,000` 단일 목록 제공
5. 레벨 20 이상 캐릭터만 공개 순위에 참가
6. 1~10위는 개별 호칭, 11위 이하는 범위 호칭을 자동 부여
7. 통신은 프로세스당 `시작 조회 1회 + 변경 시 batch 제출 1회`를 기본 상한으로 사용
8. 플레이 중 전투력 변화는 시작 스냅샷을 기준으로 사용자 화면에서 즉시 로컬 재계산
9. Top 1,000 밖에서는 정확하지 않은 예상 순위를 숫자로 표시하지 않음
10. 개발 데이터는 출시 전에 삭제하고 운영 DB를 깨끗한 상태로 시작
11. 원격 DB는 v2 병행 추가 후 앱 전환이 끝났을 때 v1 쓰기를 닫음

## 12. 전체 설계 재검토 결과

### 반드시 해결해야 하는 문제

| 우선도 | 문제 | 영향 | 대응 |
|---|---|---|---|
| P0 | 현재 앱은 랭킹 화면을 열 때 활성 슬롯 하나를 먼저 제출하고 조회함 | 시작 1회 조회, 모든 캐릭터 참가, 최소 통신 원칙과 모두 불일치 | 조회와 제출 API를 분리하고 시작 스냅샷 + batch 제출 구조로 교체 |
| P0 | 현재 DB는 레벨 1 이상을 허용하고 RPC도 레벨 20을 필터링하지 않음 | 저레벨 캐릭터가 공개 순위에 남음 | 테이블 입력 검증과 모든 공개 RPC에 `level >= 20` 강제 |
| P0 | Top 1,000만으로 1,000위 밖의 새 로컬 순위를 정확히 계산할 수 없음 | 실제보다 높은 순위를 표시할 수 있음 | Top 1,000 안에서만 숫자 재계산, 밖에서는 `1,000위 밖`과 변화 방향 표시 |
| P0 | background 콜백은 OS 강제 종료 시 네트워크 완료를 보장하지 않음 | 최신 점수가 서버에 누락될 수 있음 | 네트워크 전에 pending batch를 영속 저장하고 다음 시작에 반드시 재시도 |
| P1 | 시작 응답의 전체 Top 1,000이 약 300~600KB JSON이 될 수 있음 | 호출 수는 적지만 시작 네트워크와 파싱 비용 증가 | compact 필드, 문자열 중복 최소화, gzip, 스냅샷 버전 캐시, 실제 응답 크기 측정 |
| P1 | 1,000개 행을 완전 UI 모델과 Composable로 미리 생성 | 시작 프레임과 메모리 사용 증가 | compact 원본 보관 + LazyColumn 표시 시점 매핑 |
| P1 | `내 순위`를 `rank - 1`로 이동 | 동점자 때문에 다른 캐릭터로 이동 가능 | `public_character_id -> list_index` 매핑 사용 |
| P1 | 로컬 즉시 순위가 공식 순위처럼 보일 수 있음 | 사용자가 다음 실행에서 순위가 되돌아갔다고 느낌 | `현재 전투력 기준`과 `앱 시작 시 집계` 상태를 명확히 분리 |
| P1 | 다른 이용자의 점수는 시작 시점에 고정됨 | 로컬 1위가 실제 서버 1위와 다를 수 있음 | 즉시 순위는 시작 스냅샷 기준임을 명시하고 공식 갱신은 다음 시작에만 수행 |
| P1 | 한 세션 1회 제출 후 추가 성장 가능 | 마지막 변화가 해당 세션에 전송되지 않음 | 추가 변화는 pending으로 남겨 다음 시작에 보내며 유실하지 않음 |
| P1 | 저장된 pending에 과거 nonce나 Integrity 토큰을 함께 보관할 위험 | 재시도 시 만료·재전송 검증 실패 | 캐릭터 snapshot만 저장하고 nonce·Integrity 토큰은 실제 전송 직전에 새로 발급 |
| P1 | `current_power`가 장비 교체로 낮아지면 순위·호칭도 즉시 하락 | 사용자가 오류나 기록 박탈로 오해할 수 있음 | 내 카드에 현재 전투력과 최고 전투력을 분리 표시하고 랭킹은 현재 전투력 기준임을 고지 |
| P1 | 슬롯 ID만으로 삭제·재생성을 구분할 수 없음 | 삭제 캐릭터 점수가 새 캐릭터에 이어지거나 유령 기록이 남음 | 영구 `character_id`와 삭제 tombstone batch 도입 |
| P1 | 익명 인증 세션 분실 | 재설치 시 모든 캐릭터 순위 소유권을 잃음 | 출시 전 계정 연결/복구 범위를 결정; 미지원이면 사용자 안내 필요 |
| P2 | 1~10위 개별 호칭과 동점 | 공동 순위가 있을 때 일부 숫자 호칭이 아무에게도 부여되지 않음 | 경쟁 순위 규칙상 정상으로 정의; 같은 순위는 같은 호칭 적용 |
| P2 | 레벨 20 달성 직후 로컬 순위 | 아직 서버 참가자가 아닌데 정식 순위처럼 보일 수 있음 | `첫 집계 대기` 배지를 붙이고 공식 참가자 수에는 다음 조회 전까지 미포함 |
| P2 | 동점자 때문에 Top 1,000 응답 행 수가 정확히 1,000이 아닐 수 있음 | 1,000위 동점 일부가 잘리면 불공정 | `rank_number <= 1000`으로 동점자 전원을 포함하고 실제 행 수 증가를 허용 |

### 현재 구현에서 재사용 가능한 부분

- `displayCombatPower()` 호출과 화면용 전투력 값
- `RankingUiState`의 Loading/Content/Error+cache 상태 모델
- 동점 후보 정렬과 호칭 경계 단위 테스트 구조
- 내 순위 카드, 전체 목록, 헤더 표시와 접근성 문구 구조
- 익명 Supabase 세션과 pending session log의 로컬 이월 패턴

### 구현 전 추가로 수치 확인할 항목

- 전체 Top 1,000의 실제 JSON/gzip 크기, 파싱 시간과 RPC 실행 시간
- 참가자 1천, 1만, 10만 명에서 `rank()`와 Top 1,000 조회 실행 계획
- 전투력 변화 빈도와 한 세션 dirty 캐릭터 수
- background 전송 성공률과 다음 시작 이월률
- Top 1,000 밖 사용자가 로컬 즉시 순위 숫자를 필요로 하는 비율

## 13. 현재 확인 경계

- 확인함: 현재 Android 코드, UI 모델/테스트, 전투력 호출 경로, 로컬 SQL, 원격 마이그레이션 적용 목록
- 확인하지 않음: 원격 실데이터 행 수·점수 분포, Supabase Auth 사용자 수, 실제 RLS 공격 테스트, Play Integrity 설정, 운영 콘솔
- 변경하지 않음: 앱 코드, SQL, 원격 DB, 인증 설정, 사용자 데이터
